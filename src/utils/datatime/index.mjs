import { hoursToMilliseconds } from "./hoursToMilliseconds.mjs";
import { tokenDateExp } from "./tokenDateExp.mjs";

export {
  tokenDateExp,
  hoursToMilliseconds
}

export default {
  tokenDateExp,
  hoursToMilliseconds
}
